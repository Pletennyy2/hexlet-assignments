<h1>Tasks</h1>

<%= link_to 'New task', new_task_path %>
<hr>

<% @tasks.each do |task| %>
  <p><%= link_to task.name, task_path(task.id) %></p> <!-- можно написать просто task вместо task.id -->
  <hr>
<% end %>